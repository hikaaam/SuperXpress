export const generateValidator = (name: string) => {
    return `//generated by cli
    import * as joi from joi;    
    export const ${name}Fillable = joi.object({
      name: joi.string().required().min(10).max(100),
    });
    
    export interface I${name}Fillable {
      name: string;
    }
    `
};